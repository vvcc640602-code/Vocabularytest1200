# Google Sheets 成績紀錄設定：國中基礎 1200 單字

## 1. 建立 Google 試算表

1. 開啟 Google Sheets，建立一份新的試算表。
2. 建議命名為「國中基礎1200單字測驗成績」。
3. 到上方選單選擇「擴充功能」>「Apps Script」。

## 2. 貼上接收程式

1. 刪除 Apps Script 編輯器裡原本的內容。
2. 複製本資料夾中的 `apps-script.gs` 全部內容。
3. 貼到 Apps Script 編輯器。
4. 按「儲存」。

## 3. 部署成 Web App

1. 按右上角「部署」>「新增部署作業」。
2. 類型選擇「網頁應用程式」。
3. 執行身分選擇「我」。
4. 存取權選擇「任何人」。
5. 按「部署」並授權。
6. 複製產生的 Web App URL，通常會以 `/exec` 結尾。

## 4. 回到測驗網頁設定

1. 開啟 `index.html`。
2. 按右上角齒輪。
3. 貼上剛才複製的 Web App URL。
4. 按「儲存」。

之後學生交卷時，成績會寫入 Google 試算表的「1200單成績」工作表。

## 成績欄位

- 送出時間
- 測驗版本
- 題庫來源
- 選擇題數
- 填充題數
- 限時分鐘
- 姓名
- 測驗日期
- 花費時間
- 秒數
- 分數
- 答對題數
- 總題數
- 答錯題數
- 錯題摘要

## 更新既有 Apps Script 時

如果你已經部署過 Apps Script，請把 `apps-script.gs` 的新版內容貼到 Apps Script 編輯器後，按「部署」>「管理部署作業」>「編輯」，選擇「新增版本」後再按「部署」。

Web App URL 通常可以維持同一個，不需要重新貼到 `config.js`。
